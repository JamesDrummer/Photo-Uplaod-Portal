# Function improvements, Critical before final deployment

- Videos currently require complete download before playback, make sure to add a proper playback engine or something
- The upload button should disappear behind the lightbox when a picture or video is clicked
- Do a check for vulnrubilities and dependenciy up-to-dateness
- Add Secure Session Persistence using XOR
- Update gallery so that it detects missing files and removes them from the gallery.